<%= form_with(model: weekly_workout) do |form| %>
  <% if weekly_workout.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(weekly_workout.errors.count, "error") %> prohibited this weekly_workout from being saved:</h2>

      <ul>
        <% weekly_workout.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :member_id, style: "display: block" %>
    <%= form.text_field :member_id %>
  </div>

  <div>
    <%= form.label :week_start_date, style: "display: block" %>
    <%= form.date_field :week_start_date %>
  </div>

  <div>
    <%= form.label :complete, style: "display: block" %>
    <%= form.checkbox :complete %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
