<% content_for :title, "Attendance" %>

<section class="card">
  <h2>Attendance</h2>

  <%= form_with url: attendances_path, method: :get, local: true, class: "toolbar" do |f| %>
    <%= f.label :date, "Date" %>
    <%= f.date_field :date, value: @selected_date %>

    <%= f.label :player_id, "Player" %>
    <%= f.select :player_id, @players.collect { |p| [p.name, p.id] }, { include_blank: "(all)" }, selected: @selected_player&.id %>

    <%= f.label :view, "View" %>
    <%= f.select :view, [["Daily", "daily"], ["Weekly", "weekly"], ["Monthly", "monthly"], ["Calendar", "calendar"]], selected: @view_mode %>
    <%= f.submit "Apply" %>
  <% end %>


  <% if @selected_player %>
    <div class="attendance-summary">
      <strong><%= @selected_player.name %> attendance for <%= @selected_date.strftime('%B %Y') %></strong>
      <div class="progress-container" title="<%= @percent_attended %>%">
        <div class="progress-fill" style="width: <%= @percent_attended %>%;"></div>
      </div>
      <p><%= @percent_attended %>% of days attended this month</p>
    </div>
  <% end %>

  <% if @view_mode == "calendar" %>
    <% if @selected_player %>
      <table class="calendar">
        <thead>
          <tr>
            <% %w[Mon Tue Wed Thu Fri Sat Sun].each do |day| %>
              <th><%= day %></th>
            <% end %>
          </tr>
        </thead>
        <tbody>
          <% first = @selected_date.beginning_of_month.beginning_of_week(:monday) %>
          <% last = @selected_date.end_of_month.end_of_week(:sunday) %>
          <% (first..last).to_a.in_groups_of(7, nil) do |week| %>
            <tr>
              <% week.each do |day| %>
                <td class="<%= 'other-month' if day && day.month != @selected_date.month %>">
                  <% if day %>
                    <div class="day-number"><%= day.day %></div>
                    <% attendance = @calendar_attendances[day] %>
                    <% if attendance %>
                      <div class="status"><%= attendance.attended ? '✔️' : '✘' %></div>
                    <% else %>
                      <div class="status">–</div>
                    <% end %>
                    <% if current_user.coach == true %>
                      <%= button_to "Toggle", toggle_attendances_path(date: day, player_id: @selected_player.id), method: :patch,
                                    form: { style: "display: inline-block;" },
                                    class: "toggle-link",
                                    style: "font-size: 0.8em; padding: 2px 5px;" %>
                    <% end %>
                  <% end %>
                </td>
              <% end %>
            </tr>
          <% end %>
        </tbody>
      </table>
    <% else %>
      <h3>Team Attendance Calendar - <%= @selected_date.strftime("%B %Y") %></h3>
      <p>Shows number of players who attended each day</p>
      <table class="calendar all-players">
        <thead>
          <tr>
            <% %w[Mon Tue Wed Thu Fri Sat Sun].each do |day| %>
              <th><%= day %></th>
            <% end %>
          </tr>
        </thead>
        <tbody>
          <% first = @selected_date.beginning_of_month.beginning_of_week(:monday) %>
          <% last = @selected_date.end_of_month.end_of_week(:sunday) %>
          <% (first..last).to_a.in_groups_of(7, nil) do |week| %>
            <tr>
              <% week.each do |day| %>
                <td class="<%= 'other-month' if day && day.month != @selected_date.month %>">
                  <% if day %>
                    <div class="day-number"><%= day.day %></div>
                    <% count = @attendance_counts_by_day[day] || 0 %>
                    <div class="attendance-count">
                      <strong><%= count %></strong>
                      <% if count > 0 %>
                        <small><%= count == 1 ? "player" : "players" %></small>
                      <% else %>
                        <small>–</small>
                      <% end %>
                    </div>
                  <% end %>
                </td>
              <% end %>
            </tr>
          <% end %>
        </tbody>
      </table>
    <% end %>
  <% else %>
    <!-- Summary table view for daily, weekly, and monthly modes -->
    <table>
      <thead>
        <tr>
          <th>Player</th>
          <th>Days Attended</th>
          <th>Total Possible Days</th>
          <th>Attendance %</th>
        </tr>
      </thead>
      <tbody>
        <% total_attended = 0 %>
        <% total_possible = 0 %>
        <% @attendance_summary.each do |summary| %>
          <% total_attended += summary[:total_days_attended] %>
          <% total_possible += summary[:total_possible_days] %>
          <tr>
            <td><%= summary[:player].name %></td>
            <td><%= summary[:total_days_attended] %></td>
            <td><%= summary[:total_possible_days] %></td>
            <td><%= summary[:percent_attended] %>%</td>
          </tr>
        <% end %>
      </tbody>
      <tfoot>
        <tr style="font-weight: bold; border-top: 2px solid #333;">
          <td>TOTAL</td>
          <td><%= total_attended %></td>
          <td><%= total_possible %></td>
          <td><%= total_possible > 0 ? ((total_attended.to_f / total_possible) * 100).round(1) : 0.0 %>%</td>
        </tr>
      </tfoot>
    </table>
  <% end %>
</section>

<% unless current_user.coach? %>
  <section class="card">
    <h2>Workout Check-in</h2>
    <%= form_with model: WorkoutCheckin.new, url: workout_checkins_path do |f| %>
      <div class="field">
        <%= f.label :workout_date %>
        <%= f.date_field :workout_date, required: true %>
      </div>
      <div class="field">
        <%= f.label :proof_url, "Proof image URL" %>
        <%= f.url_field :proof_url %>
      </div>
      <%= f.submit "Save workout" %>
    <% end %>

    <h3>This month</h3>
    <ul>
      <% @workout_checkins.each do |checkin| %>
        <li><%= checkin.workout_date %> <% if checkin.proof_url.present? %>- <%= link_to "proof", checkin.proof_url, target: "_blank", rel: "noreferrer" %><% end %></li>
      <% end %>
    </ul>
  </section>
<% end %>