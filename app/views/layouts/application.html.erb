<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "D Uattendandance" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <header class="site-header">
      <h1>Team Attendance Dashboard</h1>
      <% if current_user %>
        <nav>
          <% if current_user.coach? %>
            <%= button_to "View as Player", toggle_coach_path, method: :delete, class: "link-button" %>
          <% else %>
            <%= form_with url: toggle_coach_path, method: :post, local: true, style: "display: inline-block; margin: 0 10px;" do |f| %>
              <%= f.password_field :password, placeholder: "Coach PIN", style: "width: 100px; padding: 4px; border-radius: 4px; border: 1px solid #ccc;" %>
              <%= f.submit "Enter", style: "padding: 4px 8px; cursor: pointer;" %>
            <% end %>
          <% end %>

          <%= button_to "Sign out", session_path, method: :delete, class: "link-button", data: { turbo: false } %>
        </nav>
      <% end %>
    </header>

    <% flash.each do |key, value| %>
      <p class="flash <%= key %>"><%= value %></p>
    <% end %>

    <main class="page-content">
      <%= yield %>
    </main>
  </body>
</html>